/**
 * useUploadQueue hook
 * Re-exported from UploadQueueContext to satisfy Vite Fast Refresh requirements
 * (component and hook must be in separate modules).
 */
export { useUploadQueue } from "@/contexts/UploadQueueContext";
